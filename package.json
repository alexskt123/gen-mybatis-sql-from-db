{
  "name": "try",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "lint": "eslint --fix **/*.js",
    "pretty": "npx prettier --write .",
    "check-pretty": "npx prettier --check .",
    "prepare": "husky install",
    "fix": "lint-staged",
    "test": "jest --coverage"
  },
  "dependencies": {
    "change-case": "^4.1.2",
    "commander": "^8.3.0",
    "distinct": "^0.1.0",
    "simple-node-logger": "^21.8.12",
    "xlsx": "^0.18.3"
  },
  "devDependencies": {
    "eslint": "^8.2.0",
    "husky": "^7.0.0",
    "lint-staged": "^12.3.4",
    "prettier": "2.5.1",
    "jest": "^27.5.1"
  }
}
